import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'SSAs APP',
  webDir: 'dist/MircroEmpresasFrontend',
};

export default config;
